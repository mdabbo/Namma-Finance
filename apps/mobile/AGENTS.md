# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
(This app is pinned to SDK 54 on purpose — it's the newest SDK the public Expo Go app supports. Before upgrading the SDK, check what version Expo Go on the user's phone supports.)
